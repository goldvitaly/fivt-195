#include <iostream>
#include <cstdlib>
#include <cstdio>

#include "Graph.hpp"

using namespace std;

void DFS(int vertex, Graph* graph)
{
}

int main()
{
  Graph G;
  ListIncident li;
  G.AddVertex(&li);
  /*G.AddEdge(0, 1);
  G.AddEdge(1, 2);
  G.AddEdge(2, 0);*/
  return 0;
}
